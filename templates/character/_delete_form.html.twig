<form method="post" action="{{ path('app_character_delete', {'id': character.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce personnage ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ character.id) }}">
    <button class="btn">Supprimer</button>
</form>

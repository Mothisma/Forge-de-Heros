{% extends 'base.html.twig' %}

{% block title %}Liste des personnages{% endblock %}

{% block body %}
    <h1>Liste des personnages</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Niveau</th>
                <th>Force</th>
                <th>Dextérité</th>
                <th>Constitution</th>
                <th>Intelligence</th>
                <th>Sagesse</th>
                <th>Charisme</th>
                <th>Point de vie</th>
                <th>Image</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for character in characters %}
            <tr>
                <td>{{ character.name }}</td>
                <td>{{ character.level }}</td>
                <td>{{ character.strength }}</td>
                <td>{{ character.dexterity }}</td>
                <td>{{ character.constitution }}</td>
                <td>{{ character.inteligence }}</td>
                <td>{{ character.wisdom }}</td>
                <td>{{ character.charisma }}</td>
                <td>{{ character.healthPoints }}</td>
                <td>{{ character.image }}</td>
                <td>
                    <a href="{{ path('app_character_show', {'id': character.id}) }}">Afficher</a>
                    <a href="{{ path('app_character_edit', {'id': character.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12">Aucun personnage enregistré</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_character_new') }}">Nouveau personnage</a>
{% endblock %}
